[build]
builder = "DOCKERFILE"
# buildCommand is ignored when builder is DOCKERFILE; see ./Dockerfile

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/health"
interval = 30
timeout = 5
